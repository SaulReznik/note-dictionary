import { createUseStyles } from "react-jss";

const styles = createUseStyles({
  App: {
    height: '100%',
    display: 'flex',
    flexFlow: 'column',
    textAlign: 'center',
  }
});

export default styles;
